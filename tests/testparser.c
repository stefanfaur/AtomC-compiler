struct Pt {
  int x;
  int y;
};
struct Pt points[10];

void main() {
  int sapte;
}

double max(double a, double b) {
  if (a > b) {
    a = points[3].x;
    return a;
  } else {
    return b;
  }
}

int len(char s[]) {
  int ceva;
  ceva = 0;
  while (s[ceva])
	  ceva = ceva + 1;
  return ceva;
}

int ia;

int main() {
  int doamne;
  return;
}

void main() {
  int ceva;
  int altceva;
  ceva = 10;
  if (ceva != 5) {
	puti(ceva);
  } else {
	puti(5);
  }
  while (ceva != 0) {
    puti(ceva);
    ceva = ceva / 2;
	ceva = ceva * 5;
	ceva = ceva - 1;
  }
}
